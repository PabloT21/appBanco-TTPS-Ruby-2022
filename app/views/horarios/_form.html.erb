<%= form_with(model: horario) do |form| %>
  <% if horario.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(horario.errors.count, "error") %> prohibited this horario from being saved:</h2>

      <ul>
        <% horario.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :inicio, style: "display: block" %>
    <%= form.time_field :inicio %>
  </div>

  <div>
    <%= form.label :fin, style: "display: block" %>
    <%= form.time_field :fin %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
